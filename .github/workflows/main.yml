name: CI
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Install and build
        run: 
          npm install gh-pages
          npm install
          npm build

      - name: D3pl0y!!
        run: |
          branch: gh-pages 
          folder: dist